garur/current — latest snapshot (state, sensors, nano/fault, last AI action)
garur/events — append-only AI-cycle log
garur/aiActions — append-only log of AI actions actually applied to the Hub (SET_CONFIG / CLEAR_FAULT)

Use Firebase Authentication / Realtime Database Security Rules for production. Do not leave database writes public.
